@model string;

@if (User.Identity.IsAuthenticated)
{
    <div style="position: fixed; right: 25px; bottom: 25px; z-index: 1;" class="text-white">
        <a asp-controller="@Model" asp-action="Add" class="btn btn-success">
            <i class="bi bi-plus-circle"></i> Add New
        </a>
    </div>
}
